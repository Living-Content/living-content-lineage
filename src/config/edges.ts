/**
 * Edge and connector rendering constants.
 * Standardized values for all edge types.
 */

// Edge rendering
export const EDGE_ALPHA = 0.5;

// Connector gap between workflows (pixels)
export const CONNECTOR_GAP = 100;

// Dashed connector line (workflow connections)
export const DASH_LENGTH = 8;
export const GAP_LENGTH = 6;
export const CONNECTOR_WIDTH = 2;
export const CONNECTOR_ALPHA = 0.7;
